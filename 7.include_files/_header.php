<header>
		<h1>Example of reducing code redundancy via include files</h1>
		<p>The header on this page is stored in a separate file.  This allows the same header code to be re-used on as many pages as desired, making it easier to maintain and update header code across many pages.</p>
</header>